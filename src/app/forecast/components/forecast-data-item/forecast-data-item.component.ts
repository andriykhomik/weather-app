import { Component, Input, OnInit } from '@angular/core';import { DayForecastData } from '../../../shared/interfaces/interfaces';@Component({  selector: 'app-forecast-data-item',  templateUrl: './forecast-data-item.component.html',  styleUrls: ['./forecast-data-item.component.scss'],})export class ForecastDataItemComponent implements OnInit {  @Input() public dayData!: DayForecastData;  public date: Date = new Date();  public active: boolean = false;  constructor() {}  ngOnInit(): void {    this.isToday();    console.log(this.dayData.day.avghumidity);  }  public isToday(): Date | string {    return this.date.getDate().toString() === this.dayData.date.split('-')[2]      ? 'Today'      : new Date(this.dayData.date);  }  public isActive(): boolean {    return (      (this.date.getDate() + 1).toString() === this.dayData.date.split('-')[2]    );  }}