import { Injectable } from '@angular/core';import { Astro } from '../interfaces/interfaces';@Injectable({  providedIn: 'root',})export class ConverterService {  constructor() {}  public converterAstro(primary: Astro): Astro {    return {      sunrise: this.changeTime(primary.sunrise),      sunset: this.changeTime(primary.sunset),      moonrise: this.changeTime(primary.moonrise),      moonset: this.changeTime(primary.moonset),    };  }  private changeTime(time: string): string {    let newFormatTime: any = time.split(' ');    if (newFormatTime[1] === 'AM') {      newFormatTime = newFormatTime[0];    } else if (newFormatTime[1] === 'PM') {      newFormatTime = `${+newFormatTime[0].split(':')[0] + 12}:${        newFormatTime[0].split(':')[1]      }`;    }    return newFormatTime.toString();  }}